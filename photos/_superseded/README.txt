Overrides moved aside because the shared store now has a better/kit-correct photo.

Nothing is deleted -- move a file back up one level to restore its precedence
(photos.py checks photos/ before cricket_core.headshots).

2580072.png  Nathan Ellis  moved 2026-09-02. He had no store entry and did not
             resolve by name (the CA players API caps at ~2,000 rows). Tom
             supplied CA:3531, which yielded camedia/odi/t20i/bbl, so the ODI
             packs can now show him in the right kit instead of one fixed image.
